require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
require 'mysql2'

begin
  # Устанавливаем соединение с базой данных
  client = Mysql2::Client.new(
    host: 'localhost', # адрес хоста
    username: 'root', # имя пользователя
    password: 'password', # пароль
    database: 'my_database' # название базы данных
  )

  # Пример запроса к базе данных
  results = client.query('SELECT * FROM my_table')

  # Выводим результаты запроса
  results.each do |row|
    puts row
  end

rescue Mysql2::Error => e
  puts "Ошибка при подключении к базе данных: #{e.message}"
ensure
  # Закрываем соединение с базой данных
  client.close if client
end
